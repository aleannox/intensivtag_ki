#!/bin/bash

set -ex

# Install requirements here after installing cython with the default
# requirements.txt. For e.g. pandas cython if necessary if prebuilt wheels
# are not available.

pip install -r requirements_postbuild.txt
